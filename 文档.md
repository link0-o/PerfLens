# PerfLens 通用性能分析工具技术规划书

- **文档版本：** v1.2
- **项目阶段：** Milestone 0–9 已完成 / 发布验收
- **项目类型：** Linux 性能分析工具 / MCP Server / Coding Agent Skill
- **首要支持对象：** Linux 原生应用
- **首要支持语言：** C、C++
- **后续扩展方向：** Rust、Go、Java、Python 原生扩展及混合语言应用

## 实施状态（2026-07-30）

本文档最初是分阶段规划，现同时作为实施基线。Milestone 0–9 已按顺序完成；实际命令、MCP 权限、安装和已知限制以 `README.md`、`docs/mcp-and-skill.md`、`docs/release-readiness.md` 和 `docs/troubleshooting.md` 为准。

依然不在产品范围内的能力包括 LLM API、自研 Agent Loop、Web UI、自动修改源码、Benchmark runner、生产监控和直接解析 `perf.data` 二进制格式。Google pprof 与 `perf report` 文本 Adapter 保留为后续可选扩展，不属于 Milestone 0–9 验收范围。

---

# 1. 项目定义

## 1.1 项目名称

**PerfLens**

建议英文描述：

> Evidence-driven performance analysis toolkit for Linux applications and coding agents.

建议中文描述：

> 面向 Linux 应用的证据驱动型性能分析工具，通过解析 perf、符号、调用栈、源码和基准测试数据，帮助 Coding Agent 完成性能热点定位、候选根因分析和优化验证。

---

## 1.2 项目定位

PerfLens 不是以下产品：

- 不是 perf 的简单图形界面；
- 不是火焰图生成脚本的包装；
- 不是一个只会把 `perf report` 交给大模型解释的工具；
- 不是只针对某个数据库、RPC 框架或业务系统的分析器；
- 不是一个从零实现的通用 Coding Agent；
- 不是生产监控平台；
- 不是 APM 的替代品。

PerfLens 的核心定位是：

> 将底层性能数据转换为可验证的结构化证据，再将这些证据提供给 Coding Agent，使 Agent 能够按照专业性能工程流程进行分析、源码归因和优化验证。

---

## 1.3 核心价值

传统 perf 通常能够回答：

- CPU 时间主要消耗在哪里；
- 哪些函数和调用栈占比较高；
- 程序的用户态和内核态热点；
- 某些硬件事件的统计结果。

但它通常不能直接回答：

- 热点是否异常；
- 为什么这个热点出现；
- 热点对应哪一段业务源码；
- 是计算、锁、分配、复制、系统调用、调度还是 I/O 问题；
- 当前数据是否足够支持根因判断；
- 下一步应该采集什么数据；
- 应该如何修改代码；
- 修改后性能是否真正改善；
- 性能提升是否以正确性、可靠性或持久性为代价。

PerfLens 要实现的完整链路为：

```text
原始性能数据
    ↓
标准化 Profile
    ↓
热点和调用路径
    ↓
符号及源码定位
    ↓
瓶颈初步分类
    ↓
结构化证据
    ↓
候选根因
    ↓
验证实验
    ↓
优化建议
    ↓
修改前后对比
```

---

# 2. 总体设计决策

## 2.1 产品形态

第一阶段采用以下组合：

```text
Coding Agent
Codex / Claude Code / Cursor / 其他 MCP 客户端
                       │
                       │ MCP
                       ▼
               PerfLens MCP Server
                       │
                       ▼
              PerfLens Analysis Core
                       │
       ┌───────────────┼────────────────┐
       ▼               ▼                ▼
 Profile Adapters  Symbol Resolver  Experiment Comparator
       │               │                │
 perf.data          ELF / DWARF      Benchmark / Profile
 perf script        Source Mapping      Diff
 folded stacks
 perf stat
```

同时提供一个：

```text
Performance Analysis Skill
```

Skill 用于告诉 Agent：

- 什么情况下使用 PerfLens；
- 性能分析应该按照什么顺序进行；
- 如何区分事实、推断和验证结论；
- 如何选择下一步分析工具；
- 哪些优化建议存在正确性风险；
- 报告必须包含什么内容。

---

## 2.2 为什么不先自研完整 Agent

PerfLens 的核心竞争力应该是：

- 性能数据解析；
- 调用栈处理；
- 符号解析；
- 源码定位；
- 证据组织；
- 性能数据对比；
- 专业分析工作流。

不应该把主要精力消耗在：

- Agent Loop；
- 上下文压缩；
- 对话管理；
- 通用 Memory；
- 通用 Planner；
- 通用代码编辑；
- 通用模型路由。

第一阶段直接复用成熟 Coding Agent。

MCP 提供工具能力，Skill 提供工作方法，Coding Agent 负责推理和源码修改。

---

## 2.3 是否需要独立接入 AI API

MVP 不要求 PerfLens 内部调用 AI API。

当用户通过 Codex 等 Agent 使用 PerfLens 时：

```text
Agent 自带模型
    ↓
调用 PerfLens MCP Tools
    ↓
读取结构化证据
    ↓
形成诊断和建议
```

因此第一版不要再增加：

```text
PerfLens
    ↓
OpenAI / Anthropic / 其他 LLM API
```

独立 AI API 可以作为未来的可选模块，用于：

- 独立 CLI 智能报告；
- Web 分析服务；
- CI 自动性能审查；
- PR 性能诊断机器人；
- 不依赖外部 Agent 的独立应用。

模型层必须与确定性分析核心解耦。

---

# 3. 项目目标

## 3.1 MVP 目标

这里的 MVP 指第一个可以由真实用户长期使用的产品版本，不是演示程序或一次性原型。它可以分多个可发布里程碑交付，但进入 MVP 发布状态时必须能够：

1. 读取 Linux perf 产生的离线性能数据；
2. 将不同输入转换为统一内部模型；
3. 正确区分 self 和 inclusive 样本；
4. 生成热点函数排行；
5. 提取主要调用路径；
6. 识别函数所属二进制或共享库；
7. 使用符号信息定位源码文件和行号；
8. 对热点进行通用瓶颈分类；
9. 输出结构化证据，而不是直接武断判断根因；
10. 比较两份 Profile；
11. 比较两份 Benchmark；
12. 生成 JSON 和 Markdown 报告；
13. 通过 MCP 向 Coding Agent 提供工具；
14. 提供通用 Performance Analysis Skill；
15. 在无调试符号、采样不完整等情况下明确输出限制。

每个中间里程碑也必须满足其范围内的生产质量要求，包括：

- 稳定、版本化的输入输出契约；
- 明确的错误模型和资源限制；
- 可重复的测试与性能基准；
- 升级和兼容性说明；
- 不以临时代码路径替代后续正式实现；
- 不因为“当前是早期阶段”而接受已知的数据错误、安全绕过或不可控内存增长。

---

## 3.2 中期目标

MVP 稳定后增加：

- `perf stat` 微架构指标分析；
- off-CPU Profile；
- `perf sched` 调度分析；
- `perf lock` 锁竞争分析；
- 主动运行 `perf record`；
- Profile 前后差异分析；
- Git 提交性能回退定位；
- Benchmark 自动运行；
- 修改代码后自动重新采样；
- 自动生成候选补丁；
- eBPF 数据源；
- 多语言运行时适配器。

---

## 3.3 长期目标

长期产品方向：

> AI Performance Engineer for Linux Applications

最终工作流：

```text
用户描述性能问题
    ↓
Agent 确定目标指标
    ↓
PerfLens 自动选择采样方式
    ↓
采集和解析性能数据
    ↓
定位热点及源码
    ↓
形成多个候选根因
    ↓
设计验证实验
    ↓
生成最小修改
    ↓
重新编译和测试
    ↓
比较修改前后性能
    ↓
输出已验证结论
```

---

## 3.4 工业化质量目标

PerfLens 面向个人开发者、团队内部工具链和可分发的开源产品。所有进入主分支的功能必须同时考虑：

- **正确性：** 相同输入得到稳定结果，统计口径可解释，解析失败可追踪；
- **可扩展性：** Adapter、Resolver、Artifact Store 和报告层均有稳定接口；
- **性能：** 大文件流式处理，内存消耗不随全部 frame 对象线性膨胀；
- **可靠性：** 外部进程超时、取消、异常退出和部分输出都有确定行为；
- **兼容性：** 建立 Python、Linux、perf、LLVM/elfutils 和 MCP SDK 兼容矩阵；
- **可观测性：** 记录阶段耗时、吞吐量、跳过率、未解析率和缓存命中率；
- **可维护性：** 依赖锁定、升级策略、弃用周期、Schema migration 和变更日志完整；
- **可分发性：** 支持标准 Python 包安装，并提供可复现的开发与发布流程；
- **安全性：** 最小权限、路径隔离、资源配额、日志脱敏和供应链检查默认开启。

“分阶段交付”只减少每阶段的功能范围，不降低上述质量标准。

---

# 4. 非目标范围

MVP 明确不实现：

1. 不自研完整 Agent 框架；
2. 不接入独立 LLM API；
3. 不实现 Web UI；
4. 不实现云端账号系统；
5. 不实现生产 APM；
6. 不实现持续监控；
7. 不默认附加生产进程；
8. 不默认运行需要 root 权限的命令；
9. 不直接解析所有版本的 `perf.data` 二进制格式；
10. 不支持 Windows ETW；
11. 不支持 macOS Instruments；
12. 不承诺自动确定唯一根因；
13. 不默认自动修改用户源码；
14. 不默认自动运行高负载 Benchmark；
15. 不针对特定框架写死诊断逻辑；
16. 不将高 CPU 占比直接等同于性能问题；
17. 不通过关闭 WAL、校验、同步或安全机制制造虚假性能提升；
18. 不重复实现已有成熟开源组件已经可靠解决的 ELF、DWARF、符号化、火焰图、Benchmark 运行或 MCP 协议能力；
19. 不为了赶里程碑创建只能处理示例输入、之后必须整体推翻的临时架构；
20. 不把“支持某格式”定义为只对单个 fixture 可用。

---

# 5. 目标用户和使用场景

## 5.1 目标用户

- C/C++ 开发者；
- Linux 系统开发者；
- 后端基础设施工程师；
- 数据库和存储开发者；
- 游戏引擎开发者；
- 编译器开发者；
- 高性能计算开发者；
- AI 推理基础设施开发者；
- 使用 Codex、Claude Code、Cursor 的开发者。

---

## 5.2 典型场景

### 场景 A：程序 CPU 使用率过高

用户提供：

- `perf.data`；
- 可执行文件；
- 源码仓库。

PerfLens 输出：

- Top Hotspots；
- 主要调用路径；
- 用户态和内核态占比；
- 源码文件和行号；
- 计算型、分配型、复制型等初步分类；
- 缺失证据；
- 下一步验证建议。

---

### 场景 B：某次修改导致性能下降

用户提供：

- baseline Profile；
- candidate Profile；
- 两份 Benchmark；
- 两个 Git 版本。

PerfLens 输出：

- 新增热点；
- 消失热点；
- 热点占比变化；
- 调用路径变化；
- 指标变化；
- 环境是否可比；
- 最可疑的源码区域；
- 建议的最小验证实验。

---

### 场景 C：怀疑存在锁竞争

PerfLens 不能仅因为出现：

```text
pthread_mutex_lock
futex
std::mutex::lock
```

就宣布锁竞争是根因。

正确输出应为：

- 当前 on-CPU 数据观察到什么；
- 哪些调用路径进入锁函数；
- 是否需要 `perf lock`、`perf sched` 或 off-CPU 数据；
- 当前能够形成什么级别的候选假设；
- 还缺少哪些验证证据。

---

### 场景 D：内存分配热点

PerfLens 应定位：

```text
业务函数
  → 容器扩容
  → operator new
  → malloc
```

而不是只输出：

```text
malloc 很慢，建议换 jemalloc。
```

需要分析：

- 分配来自哪些调用路径；
- 是否存在短生命周期对象；
- 是否存在字符串和容器反复扩容；
- 是否需要补充 heap profiler 数据；
- 替换 allocator 是否只是候选方案。

---

# 6. 核心原则

## 6.1 确定性分析与 AI 推理解耦

PerfLens Core 负责：

- 数据读取；
- 外部工具调用；
- 格式解析；
- 符号解析；
- 数值统计；
- 数据归一化；
- Profile 对比；
- Benchmark 对比；
- 规则匹配；
- 证据生成。

Coding Agent 负责：

- 理解用户目标；
- 选择工具；
- 阅读源码；
- 形成候选根因；
- 设计验证实验；
- 生成修改方案；
- 解释风险和权衡。

---

## 6.2 Evidence First

所有分析必须按照：

```text
Observation
    ↓
Evidence
    ↓
Hypothesis
    ↓
Validation
    ↓
Recommendation
```

禁止按照：

```text
看到函数名
    ↓
凭经验直接宣布根因
```

---

## 6.3 热点不等于根因

例如：

```text
std::sort inclusive 40%
```

可能表示：

- 排序算法本身慢；
- 调用排序次数过多；
- 输入规模异常；
- 比较函数昂贵；
- 上层业务把大量时间合理地花在排序中。

因此 PerfLens 只能把热点作为分析入口。

---

## 6.4 必须区分 Self 和 Inclusive

定义：

- **Self：** 样本直接落在当前函数内部；
- **Inclusive：** 当前函数及其所有子调用消耗的样本。

例如：

```text
parse_request inclusive 35%
malloc self 18%
```

不能解释为：

```text
parse_request 自身执行占用 35%。
```

更合理的表达是：

> 以 `parse_request` 为根的调用子树覆盖了约 35% 的样本，其中一部分直接消耗在内存分配函数中。

---

## 6.5 明确 Profile 能回答什么

on-CPU Profile 主要回答：

> 线程正在 CPU 上运行时，时间消耗在哪里。

它不能单独充分回答：

- 线程阻塞在哪里；
- 磁盘等待多长时间；
- 锁等待多长时间；
- 请求排队多长时间；
- 网络端到端延迟；
- 某个进程被调度出去多久。

证据不足时必须建议使用其他数据源。

---

## 6.6 优先集成成熟组件

PerfLens 的差异化价值是统一证据模型、跨数据源归一化、分析工作流和安全集成，不是重新实现基础设施。

任何新组件进入自研前，必须完成简短的 Build vs. Reuse 记录，至少比较：

- 功能和格式覆盖；
- 正确性与测试成熟度；
- 性能和资源消耗；
- 维护活跃度与发布节奏；
- 许可证和再分发条件；
- 安全记录与供应链风险；
- API 稳定性和替换成本；
- 离线、本地运行和隐私要求。

决策优先级：

```text
稳定标准或系统工具
    ↓
维护良好的开源库或 CLI
    ↓
薄适配层
    ↓
仅在现有方案不满足核心需求时自研
```

所有第三方组件必须封装在 Adapter 或 Provider 后面。不得让 SDK、CLI 输出格式或第三方数据模型渗透到 Analysis Core。

---

## 6.7 性能预算驱动实现

Python 是主语言，但不能假设解析和聚合天然不是性能关键路径。

项目必须建立固定的性能语料集，并持续测量：

- 解析吞吐量（输入字节/秒、样本/秒、frame/秒）；
- 峰值 RSS；
- 首个结果延迟；
- 总处理时间；
- Artifact 大小；
- 符号化吞吐量和缓存命中率。

核心实现要求：

- 流式读取和聚合，不默认把所有样本加载到内存；
- 内部热路径使用轻量对象、字符串驻留和整数 Frame ID；
- Pydantic 只用于 API、配置和 Artifact 边界，不用于每个 frame 的热循环；
- Top-N、调用路径和源码上下文均有显式上限；
- 高基数聚合默认保持精确；达到内存阈值时通过 `ArtifactStore` spill 到本地磁盘，而不是静默丢弃；
- 近似 heavy-hitter 模式只能显式开启，并必须在结果中记录算法、容量和误差界；
- 达不到已发布性能预算时，先定位瓶颈，再决定是否用 Rust 扩展替换局部热路径；
- Rust/C++ 扩展必须保持同一测试语料和输出契约，不能形成第二套分析逻辑。

初始性能预算在 Milestone 0 通过基准实测确定，不在没有数据时承诺不现实的绝对数字。

---

# 7. 总体架构

```text
                    Coding Agent / Human
                             │
                    MCP Server / CLI
                             │
                    Application Services
                 analyze / compare / report
                    ┌────────┴────────┐
                    ▼                 ▼
           Deterministic Core     Artifact Store
          models / aggregation    filesystem + index
          call paths / evidence          ▲
          comparison / rules             │
                    ▲                     │
                    └────────┬────────────┘
                             │ ports
                    Adapters / Providers
          ┌──────────────┬───────────────┬──────────────┐
          ▼              ▼               ▼              ▼
     perf/folded    LLVM/elfutils    pyelftools    benchmark formats
```

依赖规则：

- Core 只包含确定性领域模型和算法，不导入 Typer、MCP SDK、Pydantic、SQLite 或具体命令实现；
- Application Services 负责用例编排、事务边界、Artifact 生命周期和取消传播；
- Adapter/Provider 实现 Core 定义的端口，负责第三方格式和工具兼容；
- CLI 与 MCP 是同一 Application Service 的两个入口，不能复制业务逻辑；
- Artifact Store 保存版本化结果、缓存和索引；原始输入保持不可变；
- Agent 推理不进入 Core，Core 输出事实、分类候选和证据约束。

---

# 8. 技术栈

## 8.1 MVP 推荐语言

使用：

```text
Python 3.12+
```

用于：

- CLI；
- MCP Server；
- 数据模型；
- perf 文本解析；
- 结果聚合；
- JSON Schema；
- 报告生成；
- 测试。

选择 Python 的理由：

- 产品迭代和维护效率高；
- MCP SDK 和 JSON 生态成熟；
- 外部命令编排方便；
- 易于实现清晰的插件和 Adapter 边界；
- 方便 Agent 修改和测试；
- 可以在不改变公共契约的前提下，用 Rust 优化经基准确认的局部热路径。

Python 是产品主语言，不代表允许用低效对象模型处理任意规模的数据。解析器、聚合器和 Artifact 写入器必须从第一版开始采用流式设计。

不要为了体现“系统项目”而过早使用多语言架构，也不要为了维持“纯 Python”而拒绝经过数据证明有必要的局部优化。

---

## 8.2 外部工具

MVP 可调用：

- `perf`；
- `llvm-symbolizer`；
- `addr2line`；
- `eu-addr2line`；
- `readelf`；
- `nm`；
- `c++filt`；
- `git`。

所有工具必须通过适配器调用，不允许业务逻辑直接散落调用命令。

---

## 8.3 开源组件复用清单

优先复用以下成熟组件，并通过薄适配层隔离：

| 能力 | 首选组件 | 使用方式 | 不自研的内容 |
|---|---|---|---|
| Linux 采样数据读取 | Linux `perf` | 调用与 `perf.data` 匹配的系统工具导出稳定文本字段 | `perf.data` 二进制解析器 |
| 符号化 | [`llvm-symbolizer`](https://llvm.org/docs/CommandGuide/llvm-symbolizer.html) | 优先使用 JSON 输出并保持长生命周期进程 | DWARF 行表、inline 展开和 demangle 引擎 |
| 符号化 fallback | elfutils `eu-addr2line`、GNU `addr2line` | Provider fallback | 地址到源码解析算法 |
| ELF/DWARF 元数据检查 | [`pyelftools`](https://github.com/eliben/pyelftools) | 读取 ELF header、section、Build ID 和 debug 元信息；不作为高吞吐符号化主引擎 | 通用 ELF/DWARF parser |
| 火焰图 | [Brendan Gregg FlameGraph](https://github.com/brendangregg/FlameGraph) | 导出标准 folded stack 并调用现有渲染器；渲染功能为可选集成 | SVG 火焰图布局与交互 |
| 通用 Profile 交换格式 | [Google pprof `profile.proto`](https://github.com/google/pprof/blob/main/proto/profile.proto) | 中期增加输入/输出 Adapter | 自定义跨语言二进制 Profile 协议 |
| Benchmark 输入 | [pyperf](https://pyperf.readthedocs.io/)、[Google Benchmark](https://github.com/google/benchmark)、[hyperfine](https://github.com/sharkdp/hyperfine) | 解析其原生 JSON；PerfLens 自有 Schema 作为归一化层 | Benchmark runner、预热和重复测量框架 |
| MCP | [官方 MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk) | 固定受支持主版本，封装在 `perflens.mcp` | MCP 协议栈和传输层 |

复用不等于无条件依赖。每个组件必须有：

- 版本范围和 lock file；
- 许可证与 NOTICE 记录；
- 最低支持版本和兼容性测试；
- 缺失或版本不兼容时的明确错误；
- 替换 Provider 的接口；
- 对外部工具输出的 fixture 和 Golden Test；
- 安全升级和依赖更新流程。

不得复制第三方项目的大段源码后形成无人维护的私有 fork。确需修复上游问题时，优先提交上游补丁；临时补丁必须记录来源、许可证、上游 issue 和移除条件。

---

## 8.4 Python 依赖建议

核心运行依赖建议：

- Pydantic：外部数据模型、配置和 Schema 生成；
- Typer：CLI；Rich 仅用于终端展示，机器输出保持纯 JSON；
- 官方 MCP Python SDK：MCP Server；
- PyYAML：规则文件；
- Jinja2：Markdown 报告模板；
- pyelftools：ELF/DWARF 元数据检查。

开发和质量依赖建议：

- pytest、pytest-cov、pytest-timeout：测试、覆盖率和超时；
- Hypothesis：解析器、路径和聚合不变量的属性测试；
- Ruff：格式和静态检查；
- pyright：类型检查；
- `pip-audit` 或等价工具：已知漏洞检查；
- `uv` 和 lock file：可复现环境与依赖锁定。

选择依赖前必须证明它替代了真实工作量。仅为一两个简单函数不得引入大型运行时依赖。

禁止在没有明确需求前引入：

- LangChain；
- LangGraph；
- 向量数据库；
- Web 框架；
- 分布式任务队列；
- 大型 ORM；
- 数据库服务。

允许使用 Python 标准库 SQLite 作为本地 Artifact 索引和缓存，但它必须隐藏在 `ArtifactStore` 接口后，原始输入和可移植 JSON Artifact 仍是事实来源。不得要求用户部署数据库服务。

---

## 8.5 依赖与兼容性治理

仓库必须维护：

- `uv.lock`；
- 直接依赖及用途说明；
- 第三方许可证清单；
- Python、Linux 发行版、架构、perf 和 symbolizer 兼容矩阵；
- 定期依赖升级 PR；
- SDK 或外部工具大版本升级的迁移测试；
- 软件物料清单（SBOM）和发布产物哈希。

Analysis Core 不得导入 MCP SDK、Typer、Rich 或具体外部工具实现。依赖方向必须保持：

```text
CLI / MCP / Report
        ↓
Application Services
        ↓
Analysis Core
        ↑
Adapters / Providers
```

---

# 9. 输入格式

## 9.1 MVP 支持

### 输入 A：perf.data

PerfLens 不直接解析二进制格式。

处理方式：

```text
perf.data
    ↓
选择兼容的 perf Provider
    ↓
perf script 的显式字段输出
    ↓
PerfLens Parser
```

不得假设任意机器上的 `perf` 都能可靠读取任意来源的 `perf.data`。Adapter 必须先检查文件来源、架构、perf 版本、Build ID 信息和本机工具能力；无法保证兼容时必须失败并给出可操作建议，不得静默产生部分错误结果。

生产环境建议同时保存：

- 原始 `perf.data`；
- 采集命令和 `perf version`；
- `perf script` 标准化文本导出；
- Build ID 清单及必要的二进制/debug 文件清单；
- 主机、内核、架构和容器元数据；
- 所有文件的 SHA-256。

必须记录：

- perf 版本；
- 主机架构；
- 采样事件；
- 原始命令；
- 输入文件哈希；
- 解析警告。

---

### 输入 B：perf script 文本

这是 MVP 最优先实现的输入。

用途：

- 单元测试；
- Golden Test；
- 跨机器分享；
- 不依赖原始 `perf.data`；
- 方便解析器开发。

---

### 输入 C：Folded Stack

格式示例：

```text
main;worker;parse;malloc 182
main;worker;compute 271
```

用途：

- 火焰图数据；
- 快速调用栈聚合；
- 测试 self 和 inclusive 计算。

标准 Folded Stack 只包含 frame 名称、调用顺序和权重，通常不包含 DSO、PID/TID、CPU、时间戳、event 或源码地址。`FoldedStackAdapter` 必须将缺失字段显式标记为 unknown，不得从名称猜测。

Milestone 1 中的 DSO、线程和 CPU 聚合通过直接构造统一 `StackSample` 测试；这些能力不作为标准 folded 文本格式本身的承诺。如未来支持带标签的扩展 folded 格式，必须使用不同的 `source_type` 并提供独立 Schema，不能悄悄改变现有语法。

---

### 输入 D：perf report 文本

仅作为兼容输入。

限制：

- 可能缺少完整调用路径；
- 不同 perf 版本格式差异较大；
- 不应该作为内部首选格式。

---

### 输入 E：perf stat

中期支持：

- cycles；
- instructions；
- IPC；
- cache references；
- cache misses；
- branches；
- branch misses；
- context switches；
- CPU migrations；
- page faults。

---

### 输入 F：标准化开源格式

中期通过 Adapter 支持：

- Google pprof `profile.proto`；
- pyperf JSON；
- Google Benchmark JSON；
- hyperfine JSON。

这些格式先转换到 PerfLens 内部模型。内部模型不得成为另一个强迫外部工具采用的私有采集协议。

---

# 10. 统一内部数据模型

每个可以独立保存或跨进程传输的 Artifact 必须包含：

```json
{
  "schema_version": "1.0"
}
```

嵌套对象不重复存储相同版本号，由最外层 Artifact Envelope 决定解释方式。Schema 使用语义化版本：

- patch：只修正文档或放宽兼容验证；
- minor：向后兼容地增加可选字段；
- major：存在不兼容语义变化。

每次 major/minor 变更都必须提供 fixture、兼容性测试和 migration 说明。

---

## 10.1 ProfileMetadata

```json
{
  "schema_version": "1.0",
  "profile_id": "profile-uuid",
  "source_type": "perf_script",
  "input_path": "/workspace/profile.txt",
  "input_sha256": "...",
  "created_at": "...",
  "perf_version": "...",
  "producer_host": "...",
  "kernel_release": "...",
  "architecture": "x86_64",
  "event": "cycles",
  "sample_count": 100000,
  "total_weight": 100000000,
  "weight_unit": "event_count",
  "weight_source": "perf_period",
  "has_call_graph": true,
  "has_source_lines": false,
  "lost_samples": 0,
  "parse_statistics": {
    "parsed_records": 100000,
    "skipped_records": 0,
    "malformed_records": 0
  },
  "tool_versions": {},
  "warnings": []
}
```

---

## 10.2 Frame

```json
{
  "ip": "0x00007f123456",
  "address_kind": "runtime_ip",
  "module_offset": "0x123456",
  "file_offset": "0x120000",
  "symbol": "std::vector<int>::push_back",
  "raw_symbol": "std::vector<int>::push_back(int const&)",
  "normalized_symbol": "std::vector::push_back",
  "dso": "app_server",
  "dso_path": "/workspace/build/app_server",
  "build_id": "...",
  "source_file": "src/worker.cpp",
  "source_line": 128,
  "source_column": 5,
  "is_kernel": false,
  "is_inline": false,
  "is_unknown": false
}
```

---

## 10.3 StackSample

```json
{
  "sample_id": 42,
  "process_id": 100,
  "thread_id": 104,
  "thread_name": "worker-4",
  "cpu": 3,
  "timestamp": 1234.567,
  "event": "cycles",
  "weight": 1000,
  "weight_unit": "event_count",
  "weight_source": "perf_period",
  "frames": []
}
```

`sample_id` 是可选的输入内单调序号，不使用随机 UUID。聚合器不得依赖该 ID，也不得为不落盘的流式样本强制分配 ID。

需要明确调用栈方向：

```text
frames[0] = root
frames[-1] = leaf
```

所有 Adapter 都必须统一成相同方向。

---

## 10.4 Hotspot

```json
{
  "hotspot_id": "H-001",
  "symbol": "malloc",
  "dso": "libc.so.6",
  "self_weight": 15000,
  "inclusive_weight": 24000,
  "sample_count": 1200,
  "stack_occurrence_count": 1200,
  "self_percent": 15.0,
  "inclusive_percent": 24.0,
  "thread_count": 8,
  "source_locations": [],
  "top_callers": [],
  "top_callees": [],
  "categories": []
}
```

---

## 10.5 CallPath

```json
{
  "path_id": "P-001",
  "frames": [
    {"symbol": "main", "dso": "app_server"},
    {"symbol": "worker_loop", "dso": "app_server"},
    {"symbol": "parse_message", "dso": "app_server"},
    {"symbol": "malloc", "dso": "libc.so.6"}
  ],
  "weight": 12000,
  "percent": 12.0
}
```

---

## 10.6 Evidence

```json
{
  "evidence_id": "E-001",
  "type": "hotspot",
  "observation": "malloc accounts for 15% of the selected event weight",
  "source": {
    "profile_id": "profile-uuid",
    "hotspot_id": "H-001"
  },
  "reliability": "direct",
  "limitations": [
    "Allocation count and allocation size are unknown"
  ]
}
```

---

## 10.7 Hypothesis

Hypothesis 主要供 Agent 输出，Core 可以生成候选类别，但不能直接确认根因。

```json
{
  "hypothesis_id": "HP-001",
  "title": "Frequent temporary allocations may limit throughput",
  "status": "candidate",
  "confidence": "medium",
  "supporting_evidence": ["E-001", "E-002"],
  "contradicting_evidence": [],
  "missing_evidence": [
    "Allocation count",
    "Object size distribution"
  ],
  "validation_plan": []
}
```

---

# 11. Profile 解析设计

## 11.1 Adapter 接口

定义统一的流式接口：

```python
class ProfileAdapter(Protocol):
    def can_handle(self, source: ProfileSource) -> bool:
        ...

    def inspect(self, source: ProfileSource) -> ProfileMetadata:
        ...

    def open(
        self,
        source: ProfileSource,
        limits: ResourceLimits,
    ) -> ProfileStream:
        ...


class ProfileStream(Protocol):
    def __enter__(self) -> ProfileStream:
        ...

    def __iter__(self) -> Iterator[StackSample]:
        ...

    def diagnostics(self) -> ParseDiagnostics:
        ...

    def __exit__(self, *args: object) -> None:
        ...
```

实现：

```text
PerfDataAdapter
PerfScriptAdapter
FoldedStackAdapter
PerfReportAdapter
PprofAdapter
```

`ProfileStream` 必须是单遍、可关闭的流；关闭时负责终止外部进程并清理临时资源。聚合器不得假设流可以重复迭代。

`perf stat` 不产生调用栈，必须实现独立的 `MetricAdapter`，不能为了复用代码伪装成 `ProfileAdapter`。

---

## 11.2 解析容错

解析器必须：

- 支持空行；
- 支持未知 symbol；
- 支持未知 DSO；
- 支持内核 frame；
- 支持线程名包含空格；
- 支持模板函数；
- 支持被截断的符号；
- 支持没有行号；
- 支持没有 call graph；
- 对无法解析的行记录 warning；
- 不因单条损坏数据终止全部分析；
- 记录被跳过的样本数量和比例。

---

## 11.3 符号归一化

必须同时保留：

```text
raw_symbol
normalized_symbol
```

归一化可以处理：

- 地址偏移；
- `[clone]`；
- `.isra.N`；
- `.constprop.N`；
- `.cold`；
- 模板参数；
- lambda 编号；
- Rust hash；
- Go ABI 后缀。

但第一版不得为了聚合而过度删除重要语义。

---

# 12. Hotspot 聚合算法

## 12.1 Self Samples

每个样本的叶子 frame 增加：

```text
self_weight += sample.weight
```

没有 period/weight 的计数型输入使用 `weight = 1`，并将 `weight_source` 标记为 `sample_count`。

---

## 12.2 Inclusive Samples

函数级 Hotspot 的 Inclusive 默认按每个样本中的唯一聚合键计算：

```text
for key in unique((frame.symbol, frame.dso) for frame in sample.frames):
    inclusive_weight[key] += sample.weight
```

同一个调用栈中递归函数可能重复出现。

必须同时记录两个不同概念：

- `inclusive_weight`：同一聚合键在每个样本中最多累计一次，用于函数级 Inclusive 百分比；
- `stack_occurrence_count`：记录 frame 在调用栈中的出现次数，用于分析递归深度和调用树节点。

默认采用：

```text
unique_symbol_dso_per_sample
```

这样同一个递归函数不会因为在一条栈中出现多次而得到超过 100% 的函数级 Inclusive。调用树或 Call Path 中的不同递归节点仍然保留，不进行错误折叠。

计算模式必须写入 Artifact 元数据。任何改变聚合语义的选项都必须生成不同的 analysis fingerprint，不能复用旧缓存。

---

## 12.3 百分比

```text
self_percent =
    hotspot.self_weight / total_weight

inclusive_percent =
    hotspot.inclusive_weight / total_weight
```

`total_weight` 必须限定在同一个 event、进程/线程过滤范围和分析窗口中。不同 event 或不同单位的权重禁止直接合并。

函数级 Inclusive 的单项值应位于 0%–100%，但所有函数的 Inclusive 百分比之和通常大于 100%，因为一个样本会同时归属于调用链上的多个函数。

---

## 12.4 聚合维度

至少支持按以下维度聚合：

- symbol；
- symbol + DSO；
- source file；
- source line；
- process；
- thread；
- CPU；
- user/kernel；
- call path。

默认使用：

```text
symbol + DSO
```

避免不同库中的同名函数被错误合并。

---

# 13. 符号和源码解析

## 13.1 Symbol Resolver 接口

```python
class SymbolResolver(Protocol):
    def resolve(
        self,
        module: ModuleIdentity,
        location: ModuleLocation,
    ) -> list[ResolvedFrame]:
        ...
```

其中：

```text
ModuleIdentity
  - build_id
  - dso_path
  - debug_file_candidates
  - architecture

ModuleLocation
  - runtime_ip
  - module_offset
  - file_offset
  - mapping_start
  - mapping_pgoff
  - address_kind
```

仅有 `binary_path + runtime address` 不足以可靠处理 PIE、ASLR 和共享库。Adapter 必须尽可能从 perf 数据、mmap 事件和 Build ID 信息中生成模块相对位置；信息不足时返回明确限制，禁止猜测重定位基址。

解析优先级：

```text
perf 已提供 source line
    ↓
长生命周期 llvm-symbolizer（优先 JSON 输出）
    ↓
eu-addr2line
    ↓
addr2line
    ↓
保留 symbol + DSO + address
```

---

## 13.2 必须支持

- ELF；
- DWARF；
- Build ID；
- PIE；
- 共享库；
- inline frame；
- stripped binary 检测；
- 独立 debug file；
- RelWithDebInfo；
- 容器内外路径映射；
- 源码根目录映射。

Resolver 必须支持批量查询、进程复用和以 `build_id + module_offset + resolver_version` 为键的缓存。不得为每个 frame 启动一个 symbolizer 进程。

`pyelftools` 用于 ELF/DWARF 元数据检查和能力探测；高吞吐地址符号化默认委托给 LLVM/elfutils，不在 Python 中重新实现完整符号化器。

---

## 13.3 路径映射

配置示例：

```yaml
path_mappings:
  - from: /build/container/src
    to: /workspace/project
  - from: /usr/src/debug/package
    to: /workspace/dependencies/package
```

---

## 13.4 Source Context

默认返回：

- 热点行前后 20 行；
- 所属函数；
- 文件路径；
- Git commit；
- 是否第三方代码；
- 是否生成代码；
- 是否存在多个候选源码位置。

不得默认返回整个源码文件。

---

# 14. 通用瓶颈分类

分类器的目的不是确认根因，而是决定下一步应该分析什么。

## 14.1 分类类别

```text
cpu-compute
synchronization
memory-allocation
memory-copy
system-call
kernel
scheduling
io-candidate
network-candidate
serialization
runtime
garbage-collection
page-fault
unknown
```

---

## 14.2 示例规则

### 内存分配候选

匹配：

```text
malloc
calloc
realloc
free
operator new
operator delete
```

输出：

```text
观察：
内存分配相关函数具有显著 on-CPU 样本。

不能直接得出：
系统 allocator 是根因。

建议补充：
分配调用路径、次数、大小分布和对象生命周期。
```

---

### 同步候选

匹配：

```text
pthread_mutex_lock
futex
std::mutex
pthread_rwlock
```

输出：

```text
观察：
同步函数出现在主要调用路径。

限制：
on-CPU Profile 不能完整反映等待时间。

建议补充：
perf lock、perf sched 或 off-CPU 数据。
```

---

### 内存复制候选

匹配：

```text
memcpy
memmove
copy
string construction
```

建议检查：

- 数据大小；
- 调用次数；
- 临时缓冲区；
- 序列化路径；
- 是否存在重复转换；
- 是否可以减少所有权转换。

---

### 系统调用候选

匹配：

```text
read
write
send
recv
sendmsg
recvmsg
epoll
poll
fsync
```

不能直接推出：

```text
磁盘慢
网络慢
```

需要结合：

- 调用频率；
- 请求大小；
- off-CPU；
- iostat；
- 网络指标；
- 系统调用返回值。

---

## 14.3 规则格式

使用 YAML：

```yaml
id: generic-allocation-hotspot
version: 1
scope: generic
match:
  symbol_regex:
    - "(^|::)malloc$"
    - "operator new"
thresholds:
  self_percent_gte: 5
classification:
  category: memory-allocation
  confidence: low
observation:
  template: "Allocation-related functions account for significant on-CPU samples."
limitations:
  - "Allocation frequency and size are unknown."
next_steps:
  - "Inspect dominant allocation call paths."
  - "Collect allocation count and size data."
forbidden_conclusions:
  - "The system allocator is confirmed as the root cause."
```

MVP 规则必须通用。

禁止在默认规则中写死：

- 某个数据库；
- 某个 RPC 框架；
- 某个业务服务；
- 某个项目名称。

后续框架规则必须作为可选扩展包。

---

# 15. Evidence Model

## 15.1 证据等级

### L0：经验猜测

没有直接数据。

不能写入最终结论。

---

### L1：单一热点

存在函数热点，但缺少调用链或源码支持。

只能形成低置信度候选方向。

---

### L2：热点和调用路径

能够确认热点主要由哪些业务路径触发。

可以形成中低置信度假设。

---

### L3：热点、源码和辅助指标

具备：

- Profile；
- 调用路径；
- 源码；
- Benchmark 或系统指标。

可以形成高置信度候选根因。

---

### L4：A/B 验证

修改后在相同测试条件下：

- 目标指标改善；
- 对应热点下降；
- 正确性测试通过；
- 错误率没有恶化；
- 资源成本没有不可接受地转移。

只有 L4 可以标记为：

```text
Verified Improvement
```

---

## 15.2 结论状态

所有结论必须属于：

```text
observed
candidate
likely
confirmed
rejected
unknown
```

不得把 `candidate` 输出成 `confirmed`。

---

# 16. Profile 对比

## 16.1 compare\_profiles 输入

```json
{
  "baseline_profile_id": "profile-a",
  "candidate_profile_id": "profile-b",
  "group_by": "symbol_dso",
  "minimum_delta_percent": 1.0
}
```

---

## 16.2 输出

至少包含：

- 新增热点；
- 消失热点；
- Self 占比变化；
- Inclusive 占比变化；
- 排名变化；
- 调用路径变化；
- DSO 变化；
- unresolved symbol 比例变化；
- Profile 元数据差异；
- 是否具备可比性。

---

## 16.3 注意事项

两个 Profile 的百分比变化不一定代表绝对 CPU 时间变化。

例如：

```text
函数 A 从 20% 降到 10%
```

可能是：

- A 变快了；
- 其他函数变慢了；
- 样本事件不同；
- 负载不同；
- 采样时间不同。

因此 Profile 比较应尽量结合：

- 总 CPU 时间；
- Benchmark；
- 请求数量；
- Wall-clock；
- 采样事件；
- 运行时长。

---

# 17. Benchmark 对比

## 17.1 Benchmark Schema

```json
{
  "schema_version": "1.0",
  "benchmark_id": "bench-001",
  "name": "request-throughput",
  "commit": "abcdef",
  "build_type": "release",
  "duration_seconds": 60,
  "warmup_seconds": 10,
  "repetitions": 10,
  "concurrency": 16,
  "payload_bytes": 256,
  "operations": 100000,
  "metrics": {
    "throughput": {
      "unit": "operations/second",
      "higher_is_better": true,
      "values": [49800, 50100, 50050],
      "median": 50050,
      "mean": 49983.3,
      "standard_deviation": 158.8
    },
    "latency": {
      "unit": "milliseconds",
      "higher_is_better": false,
      "p50": 1.2,
      "p95": 3.8,
      "p99": 7.5
    },
    "cpu_percent": 600,
    "memory_peak_mb": 512,
    "error_count": 0
  },
  "environment": {
    "cpu_model": "...",
    "cpu_count": 16,
    "kernel": "...",
    "compiler": "...",
    "compiler_flags": ["-O3", "-g"],
    "cpu_governor": "performance",
    "turbo": true,
    "cpu_affinity": "0-7",
    "numa_policy": "...",
    "background_load": {},
    "git_dirty": false,
    "containerized": false
  },
  "source_format": "perflens"
}
```

PerfLens 优先解析 pyperf、Google Benchmark 和 hyperfine 的原生 JSON，再转换为该归一化模型。不得要求已有 Benchmark 项目改用 PerfLens 自研 runner。

---

## 17.2 可比性检查

必须比较：

- 编译模式；
- 编译器；
- 编译参数；
- CPU；
- 核数；
- NUMA；
- CPU governor、Turbo 和 affinity；
- 内核；
- 容器环境；
- 并发度；
- 数据规模；
- payload；
- warmup；
- 测试时长；
- 错误率；
- 配置；
- 后台任务。

`commit`、待测分支或补丁通常是 A/B 实验的预期自变量，必须记录但不要求相同。除明确声明的自变量外，其他条件变化均影响可比性。

结果比较必须：

- 保留每次重复测量值，不只保存单个平均数；
- 报告样本数、中心趋势、离散程度和异常值策略；
- 区分统计显著性与工程显著性；
- 支持用户配置最小实际影响阈值；
- 样本不足时输出 `insufficient_data`，不得伪造精确置信度；
- 不把单次运行的差异标记为性能提升或回退。

条件不一致时输出：

```text
Benchmark conditions differ. The metric change cannot be attributed solely to the code change.
```

---

# 18. MCP Server 设计

## 18.1 MCP 作用

MCP Server 只提供性能分析工具。

它不负责：

- 通用聊天；
- 通用代码生成；
- 自己维护对话 Memory；
- 自己执行开放式 Agent Loop。

Codex 可以通过 MCP 使用外部工具；MCP Server 也可以在初始化阶段提供 `instructions`，描述跨工具关系和推荐工作流。

`instructions` 和 Tool annotations 都是客户端提示，不是安全边界，所有权限、路径和资源限制仍必须由服务端强制执行。参见 [Codex Customization](https://developers.openai.com/codex/concepts/customization) 和 [MCP Lifecycle](https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle)。

---

## 18.2 权限分级

每个工具必须同时声明三个维度：

```text
data_effect:
  READ_ONLY | WRITES_ARTIFACTS | MUTATES_TARGET | DESTRUCTIVE

execution:
  NONE | LOCAL_PROCESS | TARGET_PROCESS | NETWORK

privilege:
  UNPRIVILEGED | ELEVATED
```

初始版本只允许：

```text
data_effect: READ_ONLY 或 WRITES_ARTIFACTS
execution: NONE 或受控的 LOCAL_PROCESS
privilege: UNPRIVILEGED
```

调用 `perf script`、symbolizer、`git` 等即属于 `LOCAL_PROCESS`，即使它不修改被分析项目。写入 PerfLens 自己的结果目录属于 `WRITES_ARTIFACTS`，不得错误标为纯 `READ_ONLY`。

MCP Tool 同时设置协议标准 annotations：

- `readOnlyHint`；
- `destructiveHint`；
- `idempotentHint`；
- `openWorldHint`。

这些字段只帮助客户端理解工具行为，服务端授权不得依赖客户端是否遵守 hint。参见 [MCP ToolAnnotations](https://modelcontextprotocol.io/specification/2025-06-18/schema)。

---

## 18.3 MVP MCP Tools

### `inspect_environment`

检查当前分析环境。

输入：

```json
{
  "workspace_root": "/workspace/project",
  "binary_path": "/workspace/project/build/app"
}
```

输出：

- OS；
- 架构；
- perf 版本；
- 可用 symbolizer；
- binary 是否存在；
- 是否带 debug info；
- 是否为 PIE；
- Build ID；
- source root；
- warnings。

---

### `analyze_profile`

输入：

```json
{
  "profile_path": "/workspace/perf.data",
  "binary_paths": [
    "/workspace/build/app"
  ],
  "source_root": "/workspace",
  "top_n": 30
}
```

输出：

- analysis ID；
- ProfileMetadata；
- Top Hotspots；
- DSO 分布；
- 用户态/内核态分布；
- unresolved symbol 比例；
- warnings；
- 建议下一步。

大结果不得全部内联到 MCP 响应。工具返回有界摘要和 Artifact 引用，详细结果通过分页工具读取。所有 Tool 定义显式的 input schema 和 output schema，并对 structured output 做服务端验证。

---

### `list_hotspots`

输入：

```json
{
  "analysis_id": "analysis-001",
  "sort_by": "self_percent",
  "limit": 30,
  "category": null
}
```

---

### `get_hotspot_details`

输出：

- Self；
- Inclusive；
- 调用者；
- 被调用者；
- 主要调用路径；
- 线程分布；
- CPU 分布；
- source locations；
- 分类结果；
- 局限性。

---

### `get_call_paths`

输入：

```json
{
  "analysis_id": "analysis-001",
  "symbol": "malloc",
  "limit": 20
}
```

输出主导调用路径。

---

### `resolve_source`

输入：

```json
{
  "build_id": "...",
  "binary_path": "/workspace/build/app",
  "module_offset": "0x123456",
  "runtime_address": "0x7f00123456",
  "source_root": "/workspace"
}
```

`runtime_address` 仅作为证据保留；存在 PIE/共享库时，解析以经过验证的模块身份和模块相对偏移为准。

输出：

- symbol；
- file；
- line；
- column；
- inline frames；
- source context；
- warnings。

---

### `get_source_context`

输入：

```json
{
  "file": "/workspace/src/parser.cpp",
  "line": 218,
  "before": 20,
  "after": 20
}
```

必须限制在 workspace 内。

---

### `classify_hotspots`

输出：

- 通用类别；
- 触发规则；
- 直接观察；
- 不允许得出的结论；
- 推荐补充数据。

---

### `compare_profiles`

比较两份分析结果。

---

### `compare_benchmarks`

比较两份 Benchmark JSON。

---

### `build_diagnosis_bundle`

这是 Agent 的高层入口。

输入：

```json
{
  "analysis_id": "analysis-001",
  "hotspot_ids": ["H-001", "H-002"],
  "include_source": true,
  "include_call_paths": true,
  "include_classification": true
}
```

输出：

```json
{
  "observations": [],
  "evidence": [],
  "candidate_categories": [],
  "limitations": [],
  "missing_evidence": [],
  "recommended_next_actions": []
}
```

该工具不直接输出唯一根因。

---

# 19. 高风险 MCP Tools 状态

以下工具不属于初始 MVP，实施状态分别记录如下。

## `collect_profile`

Milestone 9 已实现，但默认关闭。MCP 需同时开启 Artifact 写入、外部进程和主动采集，每次调用还必须提供精确授权值；PID 附加另有独立服务端和单次授权门。

执行：

- `perf record`；
- `perf stat`；
- `perf sched`；
- `perf lock`。

风险：

- 需要权限；
- 可能影响目标进程；
- 可能产生大量数据；
- 可能附加错误进程。

---

## `run_benchmark`

未实现，仍为明确非目标；PerfLens 只规范化和比较外部 Benchmark 结果。

风险：

- 运行高负载程序；
- 写入数据；
- 占满 CPU；
- 占满磁盘；
- 修改外部服务。

---

## `apply_patch`

未实现，仍为明确非目标；PerfLens 不自动修改用户源码。

第一阶段不得自动修改源码。

后期即使实现，也必须：

- 默认只生成 diff；
- 不自动提交；
- 不自动推送；
- 不绕过测试；
- 不修改安全和持久性配置。

---

# 20. Performance Analysis Skill

## 20.1 Skill 定位

Skill 不是分析引擎。

Skill 是 Agent 的性能分析操作规范。

它应告诉 Agent：

- 什么时候调用哪个 MCP Tool；
- 什么结论当前数据能够支持；
- 什么情况下必须补充数据；
- 什么建议存在风险；
- 如何生成报告。

Codex Skill 是包含 `SKILL.md` 及可选 scripts、references、assets 的可复用工作流；仓库级 Skill 放在 `.agents/skills`。参见 [Codex Customization：Skills](https://developers.openai.com/codex/concepts/customization#skills)。

---

## 20.2 建议位置

```text
.agents/
└── skills/
    └── perflens-performance-analysis/
        ├── SKILL.md
        ├── references/
        │   ├── evidence-model.md
        │   ├── on-cpu-analysis.md
        │   ├── lock-analysis.md
        │   ├── memory-analysis.md
        │   ├── syscall-analysis.md
        │   └── benchmark-validation.md
        └── assets/
            └── diagnosis-report-template.md
```

---

## 20.3 Skill 强制规则

1. 分析前确认目标指标；
2. 先检查 Profile 元数据；
3. 确认采样事件；
4. 确认是否包含调用栈；
5. 检查 unresolved symbol 比例；
6. 区分 Self 和 Inclusive；
7. 不把热点直接当成根因；
8. 不通过函数名称猜测业务逻辑；
9. 必须读取主要调用路径；
10. 能定位源码时必须定位源码；
11. 无法定位时必须说明原因；
12. 必须输出支持证据；
13. 必须输出缺失证据；
14. 锁等待不能只依赖 on-CPU Profile；
15. I/O 等待不能只依赖 on-CPU Profile；
16. 不默认建议替换 allocator；
17. 不默认建议关闭同步、校验或持久化；
18. 预计收益必须标记依据；
19. 修改后必须做相同口径 A/B 测试；
20. 正确性和错误率必须纳入验收。

---

# 21. Agent Workflow

## 21.1 默认性能诊断 Workflow

```text
1. 明确用户问题
    ↓
2. 确定目标指标
    ↓
3. 检查输入和环境
    ↓
4. 解析 Profile
    ↓
5. 检查数据质量
    ↓
6. 查看 Top Hotspots
    ↓
7. 区分 Self 和 Inclusive
    ↓
8. 钻取主要调用路径
    ↓
9. 定位业务源码
    ↓
10. 进行通用瓶颈分类
    ↓
11. 形成多个候选原因
    ↓
12. 列出支持和反对证据
    ↓
13. 指出缺失证据
    ↓
14. 设计验证实验
    ↓
15. 输出优化建议和风险
```

---

## 21.2 性能回退 Workflow

```text
1. 读取 baseline 和 candidate
2. 检查测试条件是否可比
3. 比较 Benchmark
4. 比较 Profile 元数据
5. 比较 Self 热点
6. 比较 Inclusive 热点
7. 比较调用路径
8. 识别新增开销
9. 定位相关源码改动
10. 生成候选原因
11. 设计最小验证实验
```

---

## 21.3 优化验证 Workflow

```text
1. 保存修改前 Benchmark
2. 保存修改前 Profile
3. 明确优化假设
4. 进行最小范围修改
5. 运行正确性测试
6. 使用相同参数重新 Benchmark
7. 重新采集 Profile
8. 比较指标和热点
9. 检查错误率和资源转移
10. 决定优化是否成立
```

---

# 22. 报告规范

最终 Markdown 报告必须包含：

## 22.1 问题定义

- 当前观察到的性能问题；
- 用户关注的指标；
- 测试环境；
- 输入文件。

---

## 22.2 数据质量

- Profile 类型；
- perf 版本；
- event；
- 样本数量；
- 是否有调用栈；
- 是否有源码行；
- unresolved symbols；
- 数据限制。

---

## 22.3 关键观察

只写直接从工具中得到的事实。

---

## 22.4 Top Hotspots

每个热点包括：

- symbol；
- DSO；
- Self；
- Inclusive；
- source location；
- 主要调用路径；
- thread 分布；
- 分类。

---

## 22.5 候选根因

每项包含：

- 结论状态；
- 置信度；
- 证据等级；
- 支持证据；
- 反对证据；
- 缺失证据。

---

## 22.6 建议

每条建议包括：

- 修改目标；
- 修改思路；
- 为什么可能有效；
- 正确性风险；
- 资源风险；
- 验证方式；
- 预期收益依据。

---

## 22.7 最终结论

必须使用明确措辞：

```text
Observed
Likely
Needs Validation
Confirmed
Unknown
```

---

# 23. CLI 设计

## 23.1 命令

```bash
perflens analyze-folded --input profile.folded --output analysis.json
```

```bash
perflens analyze-perf-script --input profile.perf-script --output analysis.json
```

```bash
perflens analyze-perf-data --input perf.data --output analysis.json
```

```bash
perflens inspect-elf --input ./build/app --output elf.json
perflens resolve-source --binary ./build/app --module-offset 0x1234 --output source.json
perflens source-context --file ./src/app.c --line 42 --workspace . --output context.json
```

```bash
perflens classify --analysis analysis.json --output diagnosis.json
perflens report --analysis analysis.json --output report.md
```

```bash
perflens compare-profiles \
  --baseline ./before/analysis.json \
  --candidate ./after/analysis.json \
  --output profile-comparison.json
```

```bash
perflens normalize-benchmark --input hyperfine.json --output benchmark.json
perflens compare-benchmarks \
  --baseline ./before/benchmark.json \
  --candidate ./after/benchmark.json \
  --output benchmark-comparison.json
```

```bash
perflens collect-profile \
  --mode record \
  --executable /absolute/path/to/app \
  --data-output profile.data \
  --metadata-output collection.json \
  --authorize-target \
  --authorization I_EXPLICITLY_AUTHORIZE_TARGET_PROFILING
```

MCP 使用独立入口 `perflens-mcp`，而不是 CLI 子命令。实时工具列表、启动参数和 Codex 配置见 `docs/mcp-and-skill.md`。

---

## 23.2 输出目录

```text
perflens-results/
├── manifest.json
├── metadata.json
├── hotspots.json
├── call-paths.json
├── evidence.json
├── warnings.json
├── report.md
├── index.sqlite                 # 可选本地索引，不是交换格式
├── samples.jsonl.gz             # 可选，默认不保存全部标准化样本
└── raw/
    ├── command.json
    ├── stdout.txt.gz            # 可选或按资源上限截断
    └── stderr.txt
```

`manifest.json` 是 Artifact Envelope，记录：

- Schema 和 PerfLens 版本；
- analysis fingerprint；
- 完成状态：`complete`、`partial`、`failed`；
- 每个文件的大小、SHA-256、内容类型和压缩方式；
- 输入及工具版本；
- 配置、过滤范围和聚合语义；
- 警告、截断和缺失 Artifact；
- 创建时间和阶段耗时。

默认只保留完成复现和诊断所必需的数据。保存完整标准化样本必须由配置显式开启，并受体积上限控制。

---

# 24. 错误模型

所有错误统一格式：

```json
{
  "schema_version": "1.0",
  "error": {
    "error_id": "err-...",
    "code": "SYMBOL_RESOLUTION_FAILED",
    "stage": "symbolization",
    "message": "Unable to resolve source location",
    "recoverable": true,
    "retryable": false,
    "details": {},
    "diagnostic_artifact_id": "artifact-...",
    "suggested_actions": [
      "Use a binary with debug information",
      "Provide a debug-file path"
    ]
  }
}
```

错误代码至少包括：

```text
INVALID_INPUT
UNSUPPORTED_FORMAT
PERF_NOT_FOUND
PERF_VERSION_MISMATCH
PERF_COMMAND_FAILED
PROFILE_PARSE_FAILED
CALL_GRAPH_MISSING
BINARY_NOT_FOUND
DEBUG_INFO_MISSING
SYMBOL_RESOLUTION_FAILED
SOURCE_OUTSIDE_WORKSPACE
BENCHMARK_NOT_COMPARABLE
INTERNAL_ERROR
```

不得吞掉外部命令错误。

`details` 只包含经过脱敏且有界的信息。完整 stdout/stderr 如需保留，应写入受权限控制的诊断 Artifact，不能直接塞入 MCP 响应或日志。

部分成功必须通过 Artifact 状态和 warning 表达；只有在输出仍满足声明的语义时才允许 `partial`。关键元数据缺失、单位混合或地址重定位不可信时必须失败，不能用 warning 掩盖不正确结果。

---

# 25. 安全规范

## 25.1 命令执行

禁止：

```python
subprocess.run(
    f"perf script -i {user_path}",
    shell=True
)
```

必须：

```python
subprocess.Popen(
    ["perf", "script", "-i", validated_path],
    shell=False,
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
    text=True,
    encoding="utf-8",
    errors="replace",
    start_new_session=True,
)
```

示例只表达参数数组和流式读取要求，正式实现还必须：

- 使用经过解析和 allowlist 检查的工具路径；
- 设置稳定 locale，避免本地化输出破坏解析；
- 流式消费 stdout，不使用无界 `capture_output=True`；
- 对 stdout、stderr、运行时间和临时文件设置独立上限；
- 超时或取消时先终止整个进程组，再在宽限期后强制结束；
- 检查退出码并保存有界的诊断输出；
- 不继承不必要的环境变量和文件描述符；
- 记录脱敏后的 argv、工具版本和阶段耗时。

---

## 25.2 路径安全

用户输入和工具生成路径必须：

1. 转换成绝对路径；
2. canonicalize；
3. 检查是否在对应的允许根目录；
4. 防止 `../` 越界；
5. 检查符号链接；
6. 区分只读和可写路径；
7. 禁止覆盖原始输入。

路径权限不能简化为“所有文件都必须在 workspace”。工业环境中的 debug symbols、系统共享库和工具链通常位于 workspace 外。配置必须区分：

```text
workspace_read_roots
profile_read_roots
trusted_binary_roots
trusted_debug_roots
artifact_write_root
temporary_write_root
executable_allowlist
```

默认只写 `artifact_write_root` 和 `temporary_write_root`。系统 debug 目录只能只读访问。路径映射的目标必须位于允许的源码根目录内。

---

## 25.3 资源限制

必须配置：

- 外部命令超时；
- 最大输入文件大小；
- 最大样本数；
- 最大调用路径数量；
- 最大源码上下文；
- 最大输出体积；
- 临时文件清理策略。

---

## 25.4 数据隐私

默认完全本地运行。

不得默认上传：

- 源码；
- perf 数据；
- 二进制；
- 符号；
- 本地路径；
- 环境变量；
- Benchmark；
- Git 信息。

日志必须过滤：

- Token；
- API Key；
- 密码；
- 环境变量；
- 用户主目录中的敏感路径。

---

# 26. 项目目录

```text
PerfLens/
├── AGENTS.md
├── README.md
├── LICENSE
├── CHANGELOG.md
├── pyproject.toml
├── uv.lock
├── SECURITY.md
├── THIRD_PARTY_NOTICES.md
├── .gitignore
├── .codex/
│   └── config.toml.example
├── .agents/
│   └── skills/
│       └── perflens-performance-analysis/
│           ├── SKILL.md
│           ├── references/
│           └── assets/
├── src/
│   └── perflens/
│       ├── __init__.py
│       ├── cli/
│       ├── application/
│       │   ├── analyze.py
│       │   ├── compare.py
│       │   └── services.py
│       ├── domain/
│       │   ├── models.py
│       │   ├── ports.py
│       │   └── errors.py
│       ├── contracts/
│       │   ├── artifacts.py
│       │   └── api.py
│       ├── mcp/
│       │   ├── server.py
│       │   ├── instructions.py
│       │   ├── tools/
│       │   └── schemas/
│       ├── profiles/
│       │   ├── base.py
│       │   ├── perf_data.py
│       │   ├── perf_script.py
│       │   ├── perf_report.py
│       │   ├── folded.py
│       │   └── pprof.py
│       ├── metrics/
│       │   ├── base.py
│       │   └── perf_stat.py
│       ├── integrations/
│       │   ├── commands/
│       │   ├── elf/
│       │   ├── benchmark_formats/
│       │   └── flamegraph/
│       ├── stacks/
│       ├── symbols/
│       ├── source/
│       ├── hotspots/
│       ├── callpaths/
│       ├── classification/
│       ├── evidence/
│       ├── comparisons/
│       ├── benchmarks/
│       ├── reports/
│       ├── artifacts/
│       │   ├── base.py
│       │   ├── filesystem.py
│       │   └── sqlite_index.py
│       ├── observability/
│       └── security/
├── rules/
│   ├── generic/
│   ├── linux/
│   └── cpp/
├── schemas/
│   ├── profile.schema.json
│   ├── sample.schema.json
│   ├── hotspot.schema.json
│   ├── evidence.schema.json
│   └── benchmark.schema.json
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── golden/
│   ├── security/
│   ├── compatibility/
│   ├── performance/
│   └── fixtures/
├── examples/
│   ├── cpu_bound/
│   ├── allocation_heavy/
│   ├── copy_heavy/
│   ├── lock_contention/
│   └── syscall_heavy/
└── docs/
    ├── architecture.md
    ├── profile-model.md
    ├── evidence-model.md
    ├── dependency-decisions.md
    ├── compatibility.md
    ├── performance-budget.md
    ├── mcp-tools.md
    ├── security.md
    ├── limitations.md
    └── roadmap.md
```

---

# 27. AGENTS.md 要求

项目根目录必须创建 `AGENTS.md`，用于约束 Coding Agent 的开发行为。

Codex 使用 `AGENTS.md` 保存持久的项目指导，并允许更接近工作目录的文件提供更具体的规则。参见 [Codex Customization：AGENTS Guidance](https://developers.openai.com/codex/concepts/customization#agents-guidance)。

至少包含：

```text
# Repository Goals

PerfLens is a general Linux performance-analysis toolkit.
It must not depend on any specific application, database, RPC framework,
or previous user project.

# Architecture Boundaries

- Keep deterministic analysis separate from LLM reasoning.
- Do not build a custom agent framework.
- Do not add an LLM API dependency in the MVP.
- Do not add a Web UI in the MVP.
- Do not directly parse perf.data binary formats.
- Use adapters for all external tools.

# Safety

- Never use shell=True.
- Validate and canonicalize every user-provided path.
- Keep read-only tools separate from process-execution tools.
- Never attach to a running process without explicit authorization.
- Never require root by default.
- Preserve raw command output when parsing fails.

# Quality

- All public models require schema_version.
- New parsers require fixtures and golden tests.
- New MCP tools require typed schemas and tests.
- Run lint, type checking, unit tests, and integration tests.
- Do not enter the next milestone until current acceptance tests pass.
```

---

# 28. 测试规划

## 28.1 单元测试

覆盖：

- perf script parser；
- folded stack parser；
- frame direction；
- self 计算；
- inclusive 计算；
- 递归调用；
- symbol normalization；
- DSO 区分；
- unknown frame；
- benchmark comparison；
- path validation；
- rule matching；
- error serialization。

除示例测试外，关键不变量使用属性测试覆盖：

- 任意合法样本中 `self_weight <= total_weight`；
- 函数级 `inclusive_weight <= total_weight`；
- `self_weight <= inclusive_weight`；
- 同一递归 frame 在一个样本中只贡献一次函数级 Inclusive；
- 不同 event/weight unit 不会被合并；
- parse → serialize → parse 保持语义；
- 任意路径输入都不能越过允许根目录。

---

## 28.2 Golden Tests

固定：

```text
输入 fixture
    ↓
预期 JSON
```

解析器修改后必须比较差异。

Golden 文件中应包含：

- 正常 perf script；
- 缺少 symbol；
- 缺少 DSO；
- 内核栈；
- C++ 模板；
- 递归调用；
- 多线程；
- 损坏行；
- 不同 perf 版本样式。

---

## 28.3 合成示例程序

### CPU Bound

验证：

- 计算热点；
- Self 和 Inclusive；
- 源码定位。

### Allocation Heavy

验证：

- malloc/new 分类；
- 调用路径；
- 不武断推荐 allocator。

### Copy Heavy

验证：

- memcpy/memmove；
- 上游调用点；
- 数据复制候选。

### Lock Contention

验证：

- 同步候选分类；
- 提示 on-CPU 局限；
- 建议补充 off-CPU 数据。

### Syscall Heavy

验证：

- 系统调用热点；
- 用户态/内核态；
- 不直接宣布 I/O 根因。

---

## 28.4 集成测试

至少覆盖：

```text
perf.data
    ↓
perf command adapter
    ↓
parser
    ↓
hotspot aggregation
    ↓
symbolization
    ↓
JSON report
```

---

## 28.5 MCP 测试

必须验证：

- Tool schema；
- 正常调用；
- 非法路径；
- 缺少输入；
- 过大输入；
- 超时；
- 外部工具失败；
- Artifact ID；
- 多次调用结果稳定性。

---

## 28.6 兼容性测试

CI 和发布前测试至少覆盖：

- 项目声明支持的最低和最高 Python 版本；
- x86_64；具备条件时覆盖 aarch64；
- 至少两个主流 Linux 发行版；
- 兼容矩阵中的最低和最新 perf/LLVM 版本；
- 有/无 `llvm-symbolizer`、elfutils 和 debug info；
- PIE、非 PIE、共享库、独立 debug file；
- 官方 MCP SDK 的受支持主版本；
- 上一 minor 版本生成的 Artifact。

真实 `perf.data` fixture 可能依赖架构和工具版本，必须同时保存标准化文本 fixture，避免 CI 只能在单一开发机上运行。

---

## 28.7 性能与稳定性测试

建立小、中、大三档固定语料，并在专用基准环境记录：

- 样本数、frame 数、输入体积和最大栈深；
- 冷启动与热缓存耗时；
- 峰值 RSS；
- 解析及聚合吞吐量；
- 符号化吞吐量；
- Artifact 体积；
- 重复运行的波动。

CI 使用宽松阈值防止数量级退化；严格的性能回归判断在受控机器上执行。性能基准结果保存原始重复测量值，不能只保存一条汇总数字。

还必须包含：

- 超大单行、超深调用栈和高基数 symbol 的压力测试；
- 损坏输入比例很高时的有界失败；
- 外部进程挂起、输出洪泛和取消测试；
- 长时间运行的文件描述符、子进程和临时文件泄漏测试。

---

## 28.8 依赖和供应链测试

发布流水线必须执行：

- lock file 一致性检查；
- 依赖漏洞扫描；
- 许可证策略检查；
- 构建 sdist 和 wheel；
- 在干净环境安装并运行 smoke test；
- 生成 SBOM、哈希和签名；
- 验证发布包不包含测试 Profile、源码路径或本机敏感数据。

---

# 29. 里程碑

## Milestone 0：项目骨架

实现：

- 可发布的 Python 工程；
- 分层目录和依赖方向检查；
- `AGENTS.md`；
- README；
- 数据模型；
- JSON Schema；
- Artifact Envelope 和版本策略；
- CLI 骨架；
- 测试框架；
- lint 和类型检查；
- CI 版本矩阵；
- lock file、许可证记录和依赖扫描；
- 结构化日志与阶段耗时接口；
- 开源组件 Build vs. Reuse 决策记录；
- 基础性能语料和 benchmark harness。

验收：

- 能构建 sdist/wheel，并在干净环境安装；
- CLI 可以启动；
- lint 通过；
- type check 通过；
- tests 通过；
- lock file 可复现；
- 核心层不依赖 CLI、MCP 或具体 Provider；
- 基线性能结果已记录，但不设未经测量的虚假指标；
- 发布包不包含本机路径和测试敏感数据。

---

## Milestone 1：Folded Stack 分析

实现：

- FoldedStackAdapter；
- Frame；
- StackSample；
- 单遍 ProfileStream；
- Self 聚合；
- Inclusive 聚合；
- Hotspot 排名；
- Frame 字符串驻留和整数 ID；
- JSON 输出；
- Golden Tests；
- 属性测试；
- 大文件性能测试；
- FlameGraph folded 格式兼容性 fixture。

验收：

- 能正确处理给定 folded fixtures；
- Self 和 Inclusive 结果正确；
- 递归函数在函数级 Inclusive 中每个样本只累计一次；
- 标准 folded 缺失的 DSO/线程字段被明确标记为 unknown；
- 聚合器通过直接构造 `StackSample` 验证多 DSO 同名函数和多线程隔离；
- 损坏输入提供行号、跳过率和有界诊断；
- 大 fixture 流式处理，不把所有 StackSample 保存在内存；
- 同一输入重复运行生成语义一致的 Artifact；
- 性能和峰值 RSS 基线已进入回归测试。

---

## Milestone 2：perf script 分析

实现：

- PerfScriptAdapter；
- 多线程；
- event；
- weight/period；
- DSO；
- unknown symbols；
- 调用栈；
- 容错；
- warnings；
- 显式字段契约和 perf 版本 fixture；
- 流式解析与取消。

验收：

- 能稳定解析真实 perf script fixture；
- 损坏行不会导致全部失败；
- 输出跳过比例；
- 调用栈方向统一；
- 不同 event 和 weight unit 不会错误合并；
- 在兼容矩阵中的 perf 文本 fixture 上通过；
- 吞吐量和峰值 RSS 不低于已发布性能预算。

---

## Milestone 3：perf.data Adapter

实现：

- 环境检查；
- perf Provider 选择和兼容性诊断；
- perf 版本记录；
- 安全调用系统 perf；
- 流式 stdout 和有界 stderr 保存；
- 超时；
- 取消和进程组清理；
- Build ID 与采集元数据记录；
- 输出转换为 PerfScriptAdapter。

验收：

- 能分析真实 `perf.data`；
- 不兼容的 `perf.data` 明确拒绝或给出替代导出方案；
- 外部命令失败有标准错误；
- 不使用 `shell=True`；
- 不覆盖输入文件；
- 输出洪泛、挂起和取消测试通过；
- 没有残留子进程和临时文件。

---

## Milestone 4：符号和源码定位

实现：

- ELF 信息；
- Build ID；
- debug info 检测；
- 长生命周期 LLVM symbolizer Provider；
- addr2line fallback；
- inline frame；
- source context；
- path mapping；
- `build_id + module_offset` 地址模型；
- 批量解析和符号缓存；
- pyelftools 元数据检查。

验收：

- 对带调试信息的示例程序定位到正确源码行；
- PIE、共享库和独立 debug file 测试通过；
- stripped binary 输出明确限制；
- workspace 越界被拒绝；
- 不为每个 frame 创建独立 symbolizer 进程；
- 缓存键包含模块身份和 Resolver 版本。

---

## Milestone 5：分类和证据模型

实现：

- 通用规则引擎；
- generic/linux/cpp 规则；
- Evidence；
- limitations；
- missing evidence；
- diagnosis bundle。

验收：

- 规则只产生候选分类；
- 不把规则匹配输出为确认根因；
- 每个分类都有局限性和下一步建议。

---

## Milestone 6：MCP Server

实现：

- MCP 初始化；
- server instructions；
- READ_ONLY 和 WRITES_ARTIFACTS Tools；
- 参数验证；
- input/output schema；
- 分页和有界响应；
- Artifact 管理；
- Tool annotations 和服务端强制授权；
- 错误标准化；
- 集成文档。

验收：

- MCP 客户端可以连接；
- Agent 可以调用 `analyze_profile`；
- Agent 可以继续调用 `get_hotspot_details`；
- Agent 可以获取源码上下文；
- 所有调用受允许根目录和权限策略限制；
- 大分析结果通过 Artifact 引用和分页读取；
- MCP SDK 升级不会改变 Analysis Core。

---

## Milestone 7：Performance Analysis Skill

实现：

- `SKILL.md`；
- Evidence 规则；
- 默认 Workflow；
- 报告模板；
- MCP 使用说明；
- 不应触发的场景。

验收：

- Agent 能根据 Skill 选择 PerfLens；
- 输出区分 Observation 和 Hypothesis；
- 能主动指出缺失证据；
- 不直接给危险建议。

---

## Milestone 8：Profile 和 Benchmark 对比

实现：

- compare\_profiles；
- compare\_benchmarks；
- pyperf、Google Benchmark 和 hyperfine JSON Adapter；
- 可比性检查；
- 重复测量、离散程度和实际影响阈值；
- Markdown diff 报告。

验收：

- 能发现新增热点；
- 能发现热点占比变化；
- 能警告测试环境不一致；
- 不将百分比变化直接解释为绝对时间变化；
- commit 被视为预期自变量而不是可比性失败；
- 单次测量或样本不足不会被标记为确认回退。

---

## Milestone 9：主动采样

已实现：

- collect\_profile；
- perf stat；
- perf sched；
- perf lock；
- off-CPU；
- 权限和安全确认。

验收：

- 默认禁用，CLI 与 MCP 都需要多重显式授权；
- PID 附加拥有独立授权门；
- 仅执行绝对路径并通过 allowlist 的 `perf`，不使用 shell 或 sudo；
- 目标命令路径规范化，参数不在 Artifact 中明文泄露；
- 超时、输出上限、进程组清理和不覆盖发布测试通过；
- `perf stat` 使用独立 Metric Adapter，并保留不支持的 counter；
- 真实 `perf 6.12.90` 在 `perf_event_paranoid=3` 下返回有界结构化错误且不留残余输出。

---

# 30. MVP Definition of Done

MVP 完成必须满足：

1. 支持 folded stacks；
2. 支持 perf script；
3. 支持通过兼容的 perf Provider 读取 perf.data；
4. 支持 Self 和 Inclusive；
5. 支持主要调用路径；
6. 支持 symbol + DSO 聚合；
7. 支持 ELF/DWARF 源码定位；
8. 支持未知符号和缺失调试信息；
9. 支持通用瓶颈分类；
10. 支持结构化 Evidence；
11. 支持 Profile 对比；
12. 支持 Benchmark 对比；
13. 支持 JSON 报告；
14. 支持 Markdown 报告；
15. 支持有界、版本化的 MCP Tools；
16. 提供 Performance Analysis Skill；
17. 无特定业务或框架依赖；
18. 不接入 LLM API；
19. 不自研 Agent；
20. 所有核心功能有测试；
21. 所有外部命令没有 shell 注入；
22. 所有路径受到分级允许根目录约束；
23. 报告明确标记数据局限；
24. 不将候选根因当作确认结论；
25. 能构建、安装和升级标准 Python 发布包；
26. 依赖被锁定，并完成许可证、漏洞和 SBOM 检查；
27. 支持矩阵中的 Python、perf、symbolizer 和 Linux 环境测试通过；
28. 大文件采用流式处理，并满足已发布的性能与内存预算；
29. 外部进程超时、取消、输出上限和清理测试通过；
30. Artifact Schema 有兼容性策略和迁移说明；
31. 相同输入和配置生成语义确定的结果；
32. 没有为已有成熟开源能力维护重复实现；
33. 安装、故障排查、安全模型和限制文档完整；
34. 至少使用一个非开发者本人提供的真实 Profile 完成端到端验收。

**实施结论：** 34 项 Definition of Done 均已完成或通过明确的支持矩阵验证。真实 Profile 验收使用 Brendan Gregg FlameGraph 仓库在固定提交中提供的 Linux perf 样例，出处、SHA-256、转换步骤和结果见 `docs/real-world-acceptance.md`。全部实际命令、版本矩阵、覆盖率、依赖审计、SBOM、构建和安装结果见 `docs/release-readiness.md`。

---

# 31. Agent 执行规范

开发 Agent 必须：

1. 先检查仓库现状；
2. 阅读完整规划书；
3. 阅读 `AGENTS.md`；
4. 只完成当前里程碑；
5. 不一次实现全部功能；
6. 每个阶段先写测试 fixture；
7. 再实现解析器；
8. 再运行测试；
9. 不在没有测试时重构核心模型；
10. 新增依赖前记录用途、许可证、替代方案和隔离边界；
11. 不添加 LLM API；
12. 不开发 Web UI；
13. 不开发自定义 Agent Loop；
14. 不开发生产监控；
15. 不提前实现主动采样；
16. 不添加特定框架诊断规则；
17. 任何解析失败都保留原始数据；
18. 对不确定的格式输出 warning；
19. 修改后报告全部命令和测试结果；
20. 不宣称未运行的测试已经通过；
21. 优先复用规划书指定的成熟组件，不重新实现协议、格式或符号化基础设施；
22. 不提交只为 fixture 特判的 parser；
23. 不为通过测试而降低资源限制、路径检查或错误报告；
24. 解析和聚合实现必须对真实规模输入保持流式；
25. Pydantic 仅用于边界模型，热路径使用轻量内部表示；
26. 每个里程碑都交付可维护的正式代码，不创建之后整体推翻的 demo 分支；
27. 发现上游组件缺陷时记录复现、版本和替代路径，禁止无来源复制代码；
28. 性能结论必须来自记录过环境和原始结果的 benchmark。

---

# 32. 交给 Agent 的首条任务

你现在负责从零实现 PerfLens。

PerfLens 是一个通用 Linux 性能分析工具，不属于任何已有项目，也不得依赖任何特定数据库、RPC 框架、存储引擎或业务系统。

请先完成 **Milestone 0 和 Milestone 1**，暂时不要进入其他里程碑。

具体要求：

1. 检查当前仓库，如果为空则初始化可发布的 Python 3.12 项目；
2. 创建 Milestone 0 和 1 实际需要的分层目录，不创建大量无实现的空包；
3. 创建根目录 `AGENTS.md`；
4. 创建 README；
5. 配置 `pyproject.toml`、`uv.lock`、Ruff、pyright、pytest、coverage 和 build；
6. 建立 CI，覆盖最低/最高受支持 Python 版本、lint、type check、tests 和 wheel 安装 smoke test；
7. 创建 `docs/dependency-decisions.md`，记录本阶段 Build vs. Reuse 决策；
8. 定义版本化 Artifact Envelope，以及以下边界模型：
   - ProfileMetadata；
   - Frame；
   - StackSample；
   - Hotspot；
   - CallPath；
   - Warning；
9. 为热路径定义轻量内部表示，不为每个 frame 或 sample 创建 Pydantic 模型或随机 UUID；
10. 定义 ProfileAdapter 和单遍、可关闭的 ProfileStream 抽象接口；
11. 实现兼容标准 FlameGraph folded 格式的 FoldedStackAdapter；
12. 明确标准 folded 缺少 DSO、线程、CPU、event 等元数据，并输出 unknown，不自行猜测；
13. 规定调用栈方向为 root → leaf；
14. 实现基于 `weight` 的 Self 聚合；
15. 实现基于每样本唯一 `symbol + DSO` 的函数级 Inclusive 聚合；
16. 单独记录递归 frame occurrence，不让递归函数的函数级 Inclusive 超过 100%；
17. 默认按 symbol + DSO 聚合；
18. 实现确定性 Top Hotspots 和结构化诊断；
19. 实现版本化、确定性的 JSON 输出；
20. 创建正常、递归、深栈、高基数 symbol、空输入、超长行和损坏输入的 folded fixtures；
21. 通过直接构造 `StackSample` 测试同名 symbol、多 DSO、多线程和不同 event 隔离，不伪造非标准 folded 语法；
22. 创建 unit tests、property tests、golden tests 和 security tests；
23. 创建小、中、大三档性能 fixture 或可复现生成器，记录吞吐量和峰值 RSS 基线；
24. 实现正式 CLI 的第一个命令：

```bash
perflens analyze-folded \
  --input tests/fixtures/example.folded \
  --output build/analysis.json
```

25. CLI 必须有稳定退出码、结构化错误、输入大小限制、原子写入，并拒绝覆盖原始输入；
26. 构建 sdist/wheel，在干净环境安装并执行 CLI smoke test；
27. 运行 lint、类型检查、测试、构建、依赖扫描和性能基线；
28. 更新 README，说明当前正式支持的范围、输入语义、资源限制和已知限制。

当前禁止：

- 实现 perf.data；
- 实现 perf script；
- 实现 MCP；
- 实现 Skill；
- 实现 symbolizer；
- 接入 LLM API；
- 实现主动采样；
- 实现代码修改；
- 实现 Web UI；
- 引入 LangChain 或 LangGraph；
- 使用 shell=True；
- 添加任何特定框架规则；
- 自研 ELF/DWARF parser、symbolizer、火焰图渲染器或 Benchmark runner；
- 为测试样例硬编码 symbol、调用栈或预期输出；
- 使用会随样本总量无界增长且非必要的内存缓存。

完成后输出：

1. 实际创建和修改的文件；
2. 当前架构；
3. Self 和 Inclusive 的计算方式；
4. 递归调用处理方式；
5. CLI 使用示例；
6. JSON 输出示例；
7. 执行过的测试命令；
8. 真实测试结果；
9. 构建和干净环境安装结果；
10. 性能语料、环境、吞吐量和峰值 RSS 基线；
11. 依赖、许可证和 Build vs. Reuse 决策；
12. 已知限制；
13. 进入 Milestone 2 前需要准备的内容。

除非 Milestone 0 和 Milestone 1 的全部测试通过，否则不要开始 Milestone 2。
