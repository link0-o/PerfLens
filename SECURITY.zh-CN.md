# PerfLens 安全策略

简体中文 | [English](SECURITY.md)

## 支持范围

在 1.0 之前，只为最新发布的次版本提供安全修复。已经发布的版本和标签不会被覆盖；安全修复应发布为新的版本。

## 报告安全问题

发现疑似漏洞时，请私下联系项目维护者，不要先在公开 Issue 中披露可利用细节。

报告中不要包含：

- 原始 Profile 数据；
- 私有源代码或二进制内容；
- 密钥、令牌、账号或其他凭据；
- 完整的本地绝对路径；
- 能直接识别用户、主机或内部项目的信息。

建议提供最小复现步骤、受影响版本、预期行为、实际行为和经过脱敏的错误信息。

## 默认安全边界

- PerfLens 在本地运行，不需要 LLM API。
- 默认不需要 root，也不会自行运行 sudo 或修改 perf/sysctl 策略。
- 所有用户路径都会被规范化并检查允许根目录。
- 输入 Profile 不会被覆盖。
- 解析器、诊断、源码上下文和子进程输出都有资源上限。
- 产物采用原子写入；主动采集拒绝覆盖既有输出。
- 外部程序通过参数数组启动，永远不使用 `shell=True`。
- MCP 默认关闭写入、外部进程、主动采集和 PID 附加能力。

## 主动采集与 PID 附加

主动采集必须同时满足服务器启动策略和每次调用的精确授权。附加到现有 PID 还需要独立的服务器开关与独立授权短语。

PerfLens 不会绕过 Linux 的 `perf_event_paranoid`、容器 Capability、LSM 或其他主机安全策略。权限不足时应返回有界的结构化错误，而不是尝试提权。

可选自动 Collector 是独立权限边界：MCP 只生成短期、单次、PID 身份绑定计划；Collector 使用 Unix 对等凭据和独立策略授权，只接受 PID 并写固定 spool。它在启动 perf 前检查累计字节、文件数和文件系统空闲余量，达到边界时拒绝新采集且不删除现有证据。它不接受任意命令、环境、路径或全系统目标。任何漏洞若能绕过这些约束、跨 UID 采集、逃逸 spool 或绕过配额，应按安全问题处理。

每次 Collector 往返都会双向认证。客户端固定 Socket 及其父目录身份，拒绝不安全的
写入/访问权限，要求内核 `SO_PEERCRED` UID 与 Socket 属主一致，并拒绝结构不合法、
`request_id` 不匹配或采集 PID/模式偏离授权计划的响应。仅有一个路径或格式正确的 JSON
不能证明对端是可信 Collector。

客户端不会只凭“采集成功”信任证据：它使用 `O_NOFOLLOW` 打开由计划 ID 推导的固定
spool 文件，流式复核大小上限和 SHA-256。证据必须始终是服务用户所有、与 Collector
Socket 同组、单硬链接的普通文件，权限只能是 `0640` 或 `0440`，父目录也必须由服务
用户所有且不可被组/其他用户写入。替换、软链接或组可写证据都会安全失败。

Collector 会在启动 perf 前，以原子“不存在才创建”的方式写入仅服务用户可读的零字节
计划墓碑，并同步文件与目录。墓碑在采集失败和 Collector 重启后仍保留，因此同一计划
不能靠重启服务再次执行；过期墓碑只会在后续请求中按策略的最长计划有效期回收。合法
墓碑不计入性能产物配额，也不会进入归档或清理清单；名称、属主、权限、链接数或大小
异常时所有相关操作都会拒绝继续。管理员不应手工创建、修改或删除这些隐藏状态文件。

Collector 运维日志是单行、版本化且每条最多 2 KiB 的 JSON，只记录事件类型、关联 ID、
稳定错误代码/阶段和已认证 peer UID。它不记录目标 PID、命令/环境、Profile、perf stderr、
策略或 spool 路径，也不在意外错误时输出 Python traceback。

每套 Collector 只允许一个普通用户 UID。多个调用者共享 `perflens` 组会互相看到组可读
Profile，因此策略和部署会明确拒绝多 UID，管理员也不应手工把额外用户加入该组。

## 构建与发布供应链

- 外部 GitHub Action 固定到完整提交 SHA；
- checkout 不保留 Git 凭据；
- CI 和 Release 构建任务只有仓库只读权限；
- 唯一拥有 `contents: write` 的发布任务不 checkout、不执行仓库代码；
- 它只用固定提交的官方 Action 下载同一次工作流已验证的 Bundle，再执行
  `gh release create`；
- 回归测试会拒绝可移动 Action 标签、凭据持久化或发布写权限重新扩散。

Dependabot 每周提出 Action 固定版本与 uv 锁文件更新，但不会自动合并；所有更新仍需代码
审查并通过完整 CI。发布用 wheel 和源码包使用源码提交时间独立构建两次，只有逐字节
一致时才允许继续发布。

版本标签发布会为每个可下载资产签发 SLSA Provenance。证明任务只有仓库只读、短时
OIDC 和 Attestation 写权限，不 checkout、不执行仓库代码，也不能创建 Release；发布
任务必须等待证明成功，而且拿不到证明凭据。用户安装前应通过 GitHub CLI 同时校验仓库
和 `.github/workflows/release.yml` 签发身份。

## 处理不可信输入

Profile、Benchmark JSON、ELF 文件、源码路径和持久化产物都应视为不可信输入。相关代码必须：

- 在读取前规范化路径；
- 限制输入字节数、单行长度、记录数和保留诊断；
- 不跟随路径逃逸到允许范围之外；
- 把外部工具失败转换成稳定、有限的 PerfLens 错误；
- 不在错误信息中回传大量原始数据。

实现这些边界的开发要求见[《中文开发指南》](docs/development.zh-CN.md)。
